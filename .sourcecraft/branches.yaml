branch_protection:
  policies:
    - target: default_branch
      message: "Direct changes to main are forbidden; use a pull request."
      rules:
        - prevent_force_push
        - prevent_non_pr_changes
        - prevent_deletion
